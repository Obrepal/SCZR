desc: run shell, build project
project: sczr
tags: sczr

tasks:
  build-shell:
    cmd : |
      docker build -t ${CONTAINER_NAME} .
  run-shell:
    cmd: |
      docker run -it \
      --network host \
      ${CONTAINER_NAME} bash 
