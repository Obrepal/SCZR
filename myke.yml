desc: run shell, build project
project: sczr
tags: sczr

tasks:
  build:
    cmd : |
      gcc src/${SRC_FILE_NAME} -o ${BIN_FILE_NAME} `pkg-config --cflags --libs gstreamer-1.0`
  run:
    cmd: |
      ./${BIN_FILE_NAME}
  clean:
    cmd: |
      rm -f ${BIN_FILE_NAME}
